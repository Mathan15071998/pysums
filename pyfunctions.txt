1)create a function called add (),sub(),mul(),div()
    - and get the input for a and b inside each function and then print the result.

a=int(input("Enter number 1: "))
b=int(input("Enter number 2: "))
c=a+b
d=a-b
e=a*b
f=a/b
def add ():
    print(c)
add()

def sub():
    print(d)

sub()

def mul():
    print(e)
mul()

def div():
    print(f)
div()


2)Get a integer number from user and pass it to the function called findevenorodd. Let the function print
whether the number given is odd or even.

def findevenorodd(b):
    if b%2==0:
        print("The number is even")

    else:
        print("The number is odd")
a=int(input("Enter a number: "))
findevenorodd(a)


3)Create a function called findpassorfail( ), get a input integer from user which is mark,
then find the mark entered is pass or fail.

def findpassorfail(mark):
    if mark>=35:
        print("Pass")
    else:
        print("Fail")

a=int(input("Enter the mark: "))
findpassorfail(a)


4)Get input for a and b and pass it to the function called printrange() and 
then let the function print from numbers a to b.

def printrange(a,b):
    for i in range(a,b+1):
        print(i, end=", ")

a=int(input("Enter the 1st number : "))
b=int(input("Enter the second number : "))

printrange(a,b)

5)Set s_username="EMC" and password="123"
get input for uname and password
create a function called validate()
if uname and password matches then fucntion should return true else return false.



a="EMC"
b=123

A=input("Enter your username: ")
B=int(input("Enter your password: "))

def validate():
    if a==A and b==B:
        return True
    else:
        return False
c=validate()
print(c)


6)(a+b)*C
get input for a,b and function called add () should return sum of a and b.
and multiply that sum with c.


def add (n1,n2):
    return n1+n2
a=int(input("a: "))
b=int(input("b: "))
c=int(input("c: "))

sum=add(a,b)
output=sum*c
print(output)
    

7)